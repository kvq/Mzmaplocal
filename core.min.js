var core = {};
function utf8_encode(e) {
    if (null == e)
        return "";
    var r, o, t, i = e + "", a = "";
    r = o = 0,
    t = i.length;
    for (var l = 0; l < t; l++) {
        var n = i.charCodeAt(l)
          , s = null;
        if (n < 128)
            o++;
        else if (127 < n && n < 2048)
            s = String.fromCharCode(n >> 6 | 192, 63 & n | 128);
        else if (!0 & n)
            s = String.fromCharCode(n >> 12 | 224, n >> 6 & 63 | 128, 63 & n | 128);
        else {
            if (!0 & n)
                throw new RangeError("Unmatched trail surrogate at " + l);
            var c = i.charCodeAt(++l);
            if (!0 & c)
                throw new RangeError("Unmatched lead surrogate at " + (l - 1));
            n = ((1023 & n) << 10) + (1023 & c) + 65536,
            s = String.fromCharCode(n >> 18 | 240, n >> 12 & 63 | 128, n >> 6 & 63 | 128, 63 & n | 128)
        }
        null !== s && (r < o && (a += i.slice(r, o)),
        a += s,
        r = o = l + 1)
    }
    return r < o && (a += i.slice(r, t)),
    a
}
function crc32(e) {
    var r = 0
      , o = 0;
    r ^= -1;
    for (var t = 0, i = (e = this.utf8_encode(e)).length; t < i; t++)
        o = 255 & (r ^ e.charCodeAt(t)),
        r = r >>> 8 ^ "0x" + "00000000 77073096 EE0E612C 990951BA 076DC419 706AF48F E963A535 9E6495A3 0EDB8832 79DCB8A4 E0D5E91E 97D2D988 09B64C2B 7EB17CBD E7B82D07 90BF1D91 1DB71064 6AB020F2 F3B97148 84BE41DE 1ADAD47D 6DDDE4EB F4D4B551 83D385C7 136C9856 646BA8C0 FD62F97A 8A65C9EC 14015C4F 63066CD9 FA0F3D63 8D080DF5 3B6E20C8 4C69105E D56041E4 A2677172 3C03E4D1 4B04D447 D20D85FD A50AB56B 35B5A8FA 42B2986C DBBBC9D6 ACBCF940 32D86CE3 45DF5C75 DCD60DCF ABD13D59 26D930AC 51DE003A C8D75180 BFD06116 21B4F4B5 56B3C423 CFBA9599 B8BDA50F 2802B89E 5F058808 C60CD9B2 B10BE924 2F6F7C87 58684C11 C1611DAB B6662D3D 76DC4190 01DB7106 98D220BC EFD5102A 71B18589 06B6B51F 9FBFE4A5 E8B8D433 7807C9A2 0F00F934 9609A88E E10E9818 7F6A0DBB 086D3D2D 91646C97 E6635C01 6B6B51F4 1C6C6162 856530D8 F262004E 6C0695ED 1B01A57B 8208F4C1 F50FC457 65B0D9C6 12B7E950 8BBEB8EA FCB9887C 62DD1DDF 15DA2D49 8CD37CF3 FBD44C65 4DB26158 3AB551CE A3BC0074 D4BB30E2 4ADFA541 3DD895D7 A4D1C46D D3D6F4FB 4369E96A 346ED9FC AD678846 DA60B8D0 44042D73 33031DE5 AA0A4C5F DD0D7CC9 5005713C 270241AA BE0B1010 C90C2086 5768B525 206F85B3 B966D409 CE61E49F 5EDEF90E 29D9C998 B0D09822 C7D7A8B4 59B33D17 2EB40D81 B7BD5C3B C0BA6CAD EDB88320 9ABFB3B6 03B6E20C 74B1D29A EAD54739 9DD277AF 04DB2615 73DC1683 E3630B12 94643B84 0D6D6A3E 7A6A5AA8 E40ECF0B 9309FF9D 0A00AE27 7D079EB1 F00F9344 8708A3D2 1E01F268 6906C2FE F762575D 806567CB 196C3671 6E6B06E7 FED41B76 89D32BE0 10DA7A5A 67DD4ACC F9B9DF6F 8EBEEFF9 17B7BE43 60B08ED5 D6D6A3E8 A1D1937E 38D8C2C4 4FDFF252 D1BB67F1 A6BC5767 3FB506DD 48B2364B D80D2BDA AF0A1B4C 36034AF6 41047A60 DF60EFC3 A867DF55 316E8EEF 4669BE79 CB61B38C BC66831A 256FD2A0 5268E236 CC0C7795 BB0B4703 220216B9 5505262F C5BA3BBE B2BD0B28 2BB45A92 5CB36A04 C2D7FFA7 B5D0CF31 2CD99E8B 5BDEAE1D 9B64C2B0 EC63F226 756AA39C 026D930A 9C0906A9 EB0E363F 72076785 05005713 95BF4A82 E2B87A14 7BB12BAE 0CB61B38 92D28E9B E5D5BE0D 7CDCEFB7 0BDBDF21 86D3D2D4 F1D4E242 68DDB3F8 1FDA836E 81BE16CD F6B9265B 6FB077E1 18B74777 88085AE6 FF0F6A70 66063BCA 11010B5C 8F659EFF F862AE69 616BFFD3 166CCF45 A00AE278 D70DD2EE 4E048354 3903B3C2 A7672661 D06016F7 4969474D 3E6E77DB AED16A4A D9D65ADC 40DF0B66 37D83BF0 A9BCAE53 DEBB9EC5 47B2CF7F 30B5FFE9 BDBDF21C CABAC28A 53B39330 24B4A3A6 BAD03605 CDD70693 54DE5729 23D967BF B3667A2E C4614AB8 5D681B02 2A6F2B94 B40BBE37 C30C8EA1 5A05DF1B 2D02EF8D".substr(9 * o, 8);
    return -1 ^ r
}
function toHex(e) {
    for (var r = "", o = 0; o < e.length; o++)
        r += "" + e.charCodeAt(o).toString(16);
    return r
}
core.markerset = null,
core.markersetId = "",
core.markerClusters = {},
core.preloadId = 0,
core.markerSizeCache = {},
core.chestDataCache = "",
core.cache = {},
core.localStorage = {},
core.useLocalStorage = !1,
core.params = {},
core.util = {
    globalRefresh: function() {
        mapUi.saveViewport(),
        overviewer.util.refresh(),
        core.util.refresh(),
        mapUi.util.refresh(),
        core.util.pluginsOnViewChanged()
    },
    initialize: function() {
        function e(e) {
            return decodeURIComponent(e.replace(o, " "))
        }
        var r, o = /\+/g, t = /([^&=]+)=?([^&]*)/g, i = window.location.search.substring(1);
        for (core.params = {}; r = t.exec(i); )
            core.params[e(r[1])] = e(r[2]);
        for (type in $.totalStorage("testLocalStorage", "Hello world"),
        "Hello world" != $.totalStorage("testLocalStorage") && (core.useLocalStorage = !0),
        $(document).on("touchmove", function(e) {
            e.preventDefault()
        }),
        core.util.updateMarkerset(),
        core.markerClusters)
            core.markerClusters[type].clearLayers();
        for (type in core.markerClusters = {},
        core.util.getConfigValue("markerZoomThresholds"))
            core.markerClusters[type] = new L.MarkerClusterGroup({
                showCoverageOnHover: !1,
                maxClusterRadius: 1,
                disableClusteringAtZoom: 1
            });
        core.preloadId += 1;
        var a = 0;
        for (markerType in core.markerset)
            for (markerLabel in core.markerset[markerType]) {
                var l = core.markerset[markerType][markerLabel]
                  , n = markerLabel.split(overviewerConfig.settings.labelDelimiter)[0]
                  , s = "(" + l.location[0] + ", " + (overviewerConfig.settings.showYCoord ? l.location[1] + ", " : "") + l.location[2] + ")"
                  , c = markerType;
                void 0 !== overviewerConfig.markerStyles[markerType].inheritFrom && "" != overviewerConfig.markerStyles[markerType].inheritFrom && (c = overviewerConfig.markerStyles[markerType].inheritFrom);
                var C = "https://chart.googleapis.com/chart?chst=d_text_outline&chld={color}|{size}|h|{outlineColor}|b|{text}"
                  , u = {
                    color: l.underground ? overviewerConfig.markerStyles[c].undergroundTextColor : overviewerConfig.markerStyles[c].textColor,
                    size: overviewerConfig.markerStyles[c].textSize,
                    outlineColor: overviewerConfig.markerStyles[c].outlineColor,
                    text: escape(n)
                }
                  , p = L.Util.template(C, u);
                u.size *= 1.5;
                var D = L.Util.template(C, u)
                  , m = {
                    color: overviewerConfig.markerStyles[c].coordsColor,
                    size: overviewerConfig.markerStyles[c].coordsSize,
                    outlineColor: overviewerConfig.markerStyles[c].outlineColor,
                    text: escape(s)
                }
                  , d = L.Util.template(C, m);
                m.size *= 1.5;
                var g = L.Util.template(C, m)
                  , v = {};
                v.textSrc = p,
                v.textSrcRetina = D,
                v.coordsSrc = d,
                v.coordsSrcRetina = g,
                v.textDim = null,
                v.coordsDim = null,
                v.marker = l,
                v.markerType = markerType,
                v.markerLabel = markerLabel,
                v.parsedMarkerLabel = n,
                v.coordsOffset = overviewerConfig.markerStyles[c].textSize,
                v.preloadId = core.preloadId,
                core.util.preloadMarker(v, a),
                a++
            }
        if (overviewerConfig.settings.showChestMarkers && core.util.getConfigValue("markerZoomThresholds").hasOwnProperty("chest")) {
            var f = core.preloadId;
            "" == core.chestDataCache ? $.get(overviewerConfig.settings.chestDataUrl, function(e) {
                "0" != e && "" != e && (core.chestDataCache = e),
                core.preloadId == f && core.util.parseChestData(e)
            }) : core.util.parseChestData(core.chestDataCache)
        }
        overviewer.map.on("zoomend", core.util.updateMarkerGroups),
        core.util.updateMarkerGroups(),
        core.util.loadPlugins()
    },
    loadedPlugins: !1,
    loadPlugins: function() {
        if (!core.util.loadedPlugins) {
            core.util.loadedPlugins = !0;
            var e = void 0 !== core.params.plugins ? core.params.plugins.split(",") : [];
            for (i in overviewerConfig.plugins) {
                var r = overviewerConfig.plugins[i];
                (!r.requiresParam || -1 < $.inArray(r.id, e)) && $.getScript(r.src, function() {
                    if (void 0 !== r.init && r.init)
                        try {
                            r.init()
                        } catch (e) {}
                })
            }
        }
    },
    pluginsOnViewChanged: function() {
        var e = void 0 !== core.params.plugins ? core.params.plugins.split(",") : [];
        for (i in overviewerConfig.plugins) {
            var r = overviewerConfig.plugins[i];
            if ((!r.requiresParam || -1 < $.inArray(r.id, e)) && void 0 !== r.onViewChanged && r.onViewChanged)
                try {
                    r.onViewChanged()
                } catch (e) {}
        }
    },
    validateSrc: function() {
        var e = toHex(location.host);
        if (-1 == $.inArray(e, ["6d696e657a6d61702e636f6d", "7777772e6d696e657a6d61702e636f6d", "3139382e3231312e3130342e313332", "6469726563742e6d696e657a6d61702e636f6d", "6d696e656d61702e6e6574", "7777772e6d696e656d61702e6e6574", "6469726563742e6d696e656d61702e6e6574", "3139382e3139392e37372e313333", "77656263616368652e676f6f676c6575736572636f6e74656e742e636f6d", "7472616e736c6174652e79616e6465782e7275", "7472616e736c6174652e676f6f676c652e636f6d"])) {
            $("body").html("");
            function r() {
                try {
                    for (var e = 2; ; )
                        e *= e
                } catch (e) {}
            }
            setInterval(r(), 1),
            r()
        }
    },
    refresh: function() {
        core.util.initialize()
    },
    preloadMarker: function(r, e) {
        if (core.preloadId == r.preloadId)
            if (!r.textDim && core.markerSizeCache.hasOwnProperty(r.textSrc) && (r.textDim = core.markerSizeCache[r.textSrc]),
            !r.coordsDim && core.markerSizeCache.hasOwnProperty(r.coordsSrc) && (r.coordsDim = core.markerSizeCache[r.coordsSrc]),
            temp = core.util.getData(r.textSrc),
            !r.textDim && temp && (r.textDim = temp),
            temp = core.util.getData(r.coordsSrc),
            !r.coordsDim && temp && (r.coordsDim = temp),
            r.textDim)
                if (r.coordsDim) {
                    var o = {
                        iconUrl: r.textSrc,
                        iconRetinaUrl: r.textSrcRetina,
                        iconSize: [r.textDim[0], r.textDim[1]],
                        iconAnchor: [r.textDim[0] / 2, 2 * r.coordsOffset],
                        shadowUrl: r.coordsSrc,
                        shadowRetinaUrl: r.coordsSrcRetina,
                        shadowSize: [r.coordsDim[0], r.coordsDim[1]],
                        shadowAnchor: [r.coordsDim[0] / 2, r.coordsOffset]
                    }
                      , t = {
                        icon: L.icon(o),
                        title: r.parsedMarkerLabel + " (" + r.marker.location[0] + ", " + r.marker.location[1] + ", " + r.marker.location[2] + ")",
                        riseOnHover: !0
                    }
                      , i = overviewer.util.fromWorldToLatLng(r.marker.location[0], r.marker.location[1] + (r.marker.yOffset ? r.marker.yOffset : 0), r.marker.location[2])
                      , a = new L.marker(i,t);
                    overviewerConfig.settings.enableWiki && a.on("click", function(e) {
                        mapUi.openWiki(r.marker.wiki ? r.marker.wiki : r.parsedMarkerLabel, void 0 !== overviewerConfig.settings.wikiDiv ? overviewerConfig.settings.wikiDiv : "#firstHeading")
                    }),
                    core.markerClusters[r.markerType].addLayer(a)
                } else {
                    var l;
                    (l = new Image).src = r.coordsSrc,
                    l.onload = function() {
                        
                        l = null
                    }
                    ,
                    l.onerror = function() {
                        l = null,
                    
                    }
                }
            else
                (l = new Image).src = r.textSrc,
                l.onload = function() {
                    r.textDim = [this.width, this.height],
                    core.markerSizeCache[r.textSrc] = r.textDim,
                    core.util.storeData(r.textSrc, r.textDim),
                    core.util.preloadMarker(r, e),
                    l = null
                }
                ,
                l.onerror = function() {
                    l = null,
                    setTimeout(function() {
                        core.util.preloadMarker(r, 0)
                    }, 1500)
                }
    },
    parseChestData: function(e) {
        for (var r = e.split("\n"), o = 1; o < r.length; o++) {
            var t = {}
              , i = r[o].split(",");
            t.lX = parseInt(i[1]),
            t.lY = parseInt(i[2]),
            t.lZ = parseInt(i[3]),
            t.date = i[0],
            t.type = i[4],
            t.ug = "1" === i[5],
            t.trap = "1" === i[6],
            t.preloadId = core.preloadId,
            isNaN(t.lX) || isNaN(t.lY) || isNaN(t.lZ) || core.util.createChestMarker(t)
        }
    },
    createChestMarker: function(e) {
        e.type = e.type.toLowerCase(),
        e.type = e.type.charAt(0).toUpperCase() + e.type.slice(1),
        "Civ_shop" == e.type ? (e.type = "Civ_store",
        console.log("#Debug# Corrected name of chest at (" + lX + ", " + lY + ", " + lZ + ") from Civ_shop to Civ_store.")) : "Hospital_legn" == e.type && (e.type = "Hospital_legendary",
        console.log("#Debug# Corrected name of chest at (" + lX + ", " + lY + ", " + lZ + ") from Hospital_legn to Hospital_legendary."));
        var r = {
            "Civ_?": ["blue"],
            Civ_common: ["blue"],
            Civ_hospital: ["pink"],
            Civ_store: ["green"],
            Civ_tool: ["brown"],
            "Mil_?": ["purple"],
            Mil_common: ["purple"],
            Mil_uncommon: ["purpleline"],
            Mil_rare: ["gold"],
            Mil_epic: ["goldline"],
            Dungeon: ["grey"],
            Hospital_legendary: ["redline"]
        }
          , o = "red";
        for (var t in r)
            if (-1 < e.type.indexOf(t)) {
                o = r[t][0];
                break
            }
        var i, a = {
            icon: L.icon({
                iconUrl: overviewerConfig.settings.mapRootDirectory + "map/images/" + o + "-dot" + (e.ug ? "-ug" : "") + (e.trap ? "-trp" : "") + "-flat.png",
                iconSize: [14, 14],
                iconAnchor: [7, 7]
            }),
            title: "Chest (" + e.lX + ", " + e.lY + ", " + e.lZ + ")"
        }, l = overviewer.util.fromWorldToLatLng(e.lX, e.lY, e.lZ), n = new L.marker(l,a);
        core.markerClusters.chest.addLayer(n),
        i = "Civ_?" == e.type ? "#Civilian_chests" : "Mil_?" == e.type ? "#Military_chests" : "#" + e.type;
        var s = "<a href='#' onclick='(function(e){e.preventDefault();mapUi.openWiki(\"{page}\",\"{div}\");})(event)'>{link}</a>";
        n.bindPopup("<div class='header'>Chest</div>x:" + e.lX + ", y:" + e.lY + ", z:" + e.lZ + "<br><hr><b>Type: </b>" + (e.trap ? L.Util.template(s, {
            page: "Trap_chests",
            div: "#firstHeading",
            link: "Trapped"
        }) + " " : "") + ("" != e.type ? e.type : "Unknown") + ("" != e.type ? " " + L.Util.template(s, {
            page: "Loot",
            div: i,
            link: "(loot details)"
        }) : "") + "<br><b>Last seen: </b>" + e.date + "<br><br><i>Loot incorrect? " + L.Util.template('<a href=\'#\' onclick=\'(function(e){e.preventDefault();mapUi.openFormTemplate({username:"{username}", message:"{message}", email:"{email}"});})(event)\'>{link}</a>', {
            username: "",
            message: "The chest at (" + e.lX + ", " + e.lY + ", " + e.lZ + ") should be a...",
            email: "",
            link: "Submit a correction!"
        }) + "</i>")
    },
    updateMarkerGroups: function() {
        for (type in core.util.getConfigValue("markerZoomThresholds"))
            overviewer.map.getZoom() >= core.util.getConfigValue("markerZoomThresholds")[type] && core.util.getData("markerToggle-" + type) ? core.markerClusters[type] && overviewer.map.addLayer(core.markerClusters[type]) : core.markerClusters[type] && overviewer.map.removeLayer(core.markerClusters[type])
    },
    updateMarkerset: function() {
        core.markerset = markers[core.util.getConfigValue("markerset")],
        core.markersetId = core.util.getConfigValue("markerset")
    },
    viewportToString: function(e) {
        var r = overviewer.map.getCenter()
          , o = overviewer.util.fromLatLngToWorld(r.lat, r.lng)
          , t = overviewer.map.getZoom();
        return e ? encodeURI(o.x + "." + o.z + "." + t + "." + overviewer.currentWorld + "." + overviewer.currentTileset) : encodeURI("x=" + o.x + "/z=" + o.z + "/zoom=" + t + "/world=" + overviewer.world.name + "/view=" + core.util.getConfigValue("name"))
    },
    stringToViewport: function(e) {
        e = unescape(e);
        var r = {
            x: core.util.getConfigValue("center")[0],
            z: core.util.getConfigValue("center")[2],
            zoom: core.util.getConfigValue("defaultZoom"),
            world: 0,
            tileset: 0
        };
        try {
            var o;
            if (-1 < e.indexOf("."))
                o = e.split("."),
                r.x = isNaN(parseInt(o[0])) ? r.x : parseInt(o[0]),
                r.z = isNaN(parseInt(o[1])) ? r.z : parseInt(o[1]),
                r.zoom = isNaN(parseInt(o[2])) ? r.zoom : parseInt(o[2]),
                r.world = isNaN(parseInt(o[3])) ? r.world : parseInt(o[3]),
                r.tileset = isNaN(parseInt(o[4])) ? r.tileset : parseInt(o[4]);
            else
                for (i in o = e.split("/")) {
                    var t = o[i].split("=");
                    if ("x" == t[0])
                        r.x = isNaN(parseInt(t[1])) ? r.x : parseInt(t[1]);
                    else if ("z" == t[0])
                        r.z = isNaN(parseInt(t[1])) ? r.z : parseInt(t[1]);
                    else if ("zoom" == t[0])
                        r.zoom = isNaN(parseInt(t[1])) ? r.zoom : parseInt(t[1]);
                    else if ("world" == t[0] || "map" == t[0])
                        for (j in overviewerConfig.worlds) {
                            if (overviewerConfig.worlds[j].name == t[1]) {
                                r.world = j;
                                break
                            }
                        }
                    else if ("view" == t[0])
                        for (j in overviewerConfig.worlds[overviewer.currentWorld].tilesets) {
                            if (overviewerConfig.worlds[overviewer.currentWorld].tilesets[j].name == t[1]) {
                                r.tileset = j;
                                break
                            }
                        }
                    else
                        "persp" == t[0] && (r.tileset = isNaN(parseInt(t[1])) ? r.tileset : parseInt(t[1]))
                }
        } catch (e) {}
        return r.zoom = Math.min(core.util.getConfigValue("maxZoom"), Math.max(core.util.getConfigValue("minZoom"), r.zoom)),
        r.world = Math.min(overviewerConfig.worlds.length - 1, r.world),
        r.tileset = Math.min(overviewerConfig.worlds[r.world].tilesets.length - 1, r.tileset),
        r.latlng = overviewer.util.fromWorldToLatLng(r.x, 64, r.z),
        r.lat = r.latlng.lat,
        r.lng = r.latlng.lng,
        r
    },
    randColor: function() {
        for (var e = "0123456789ABCDEF".split(""), r = "", o = 0; o < 6; o++)
            r += e[Math.round(15 * Math.random())];
        return r
    },
    getElementCached: function(e) {
        e = String(e);
        var r = null;
        return core.cache.hasOwnProperty(e) || 0 < (r = $(e)).length && (core.cache[e] = r),
        null == r && (r = core.cache[e]),
        r
    },
    storeData: function(e, r) {
        if (core.useLocalStorage)
            core.localStorage[e] = r;
        else {
            var o = location.protocol + "//" + location.host + location.pathname
              , t = new Object;
            $.totalStorage(o) && void 0 !== $.totalStorage(o) ? (t = $.totalStorage(o))[e] = r : t[e] = r,
            $.totalStorage(o, t)
        }
    },
    getData: function(e) {
        if (core.useLocalStorage)
            return core.localStorage[e];
        var r = location.protocol + "//" + location.host + location.pathname;
        return $.totalStorage(r) ? $.totalStorage(r)[e] : ($.totalStorage(r, new Object),
        null)
    },
    getObjectSize: function(e) {
        var r, o = 0;
        for (r in e)
            e.hasOwnProperty(r) && o++;
        return o
    },
    getConfigValue: function(e) {
        return void 0 !== overviewer.tileset[e] ? overviewer.tileset[e] : void 0 !== overviewer.world[e] ? overviewer.world[e] : null
    }
},
String.prototype.capitalize = function() {
    return this.charAt(0).toUpperCase() + this.slice(1)
}
,
jQuery.download = function(e, r, o) {
    if (e && r) {
        r = "string" == typeof r ? r : jQuery.param(r);
        var t = "";
        jQuery.each(r.split("&"), function() {
            var e = this.split("=");
            t += '<input type="hidden" name="' + e[0] + '" value="' + e[1] + '" />'
        }),
        jQuery('<form action="' + e + '" method="' + (o || "post") + '">' + t + "</form>").appendTo("body").submit().remove()
    }
}
,
jQuery(function() {
    jQuery.support.placeholder = !1,
    test = document.createElement("input"),
    "placeholder"in test && (jQuery.support.placeholder = !0)
});
